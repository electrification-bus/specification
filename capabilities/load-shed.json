{
  "$schema": "https://ebus.energy/schemas/property-catalog.json",
  "schema_version": "property-schema-v1",
  "kind": "capability-catalog",
  "capability": "energy.ebus.capability.load-shed",
  "version": "0.3",
  "status": "DRAFT",
  "date": "2026-07-30",
  "properties": {
    "priority": {
      "datatype": "enum",
      "format": "UNKNOWN,NEVER,OFF_GRID,SOC_THRESHOLD",
      "settable": true,
      "req": "SHOULD",
      "description": "The circuit's shedding class. Baseline (every host): `UNKNOWN`, `NEVER`, `OFF_GRID`. Optional additional triggers are advertised in the property's `$format`: `SOC_THRESHOLD` and future spec- or vendor-defined values."
    }
  }
}
